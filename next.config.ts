// next.config.js
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Must be empty or standard Next.js config here. 
  // NO 'webpack' or 'turbopack' properties.
};

export default nextConfig;

